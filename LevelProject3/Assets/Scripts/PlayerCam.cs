using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerCam : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //this script is for player pov
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
